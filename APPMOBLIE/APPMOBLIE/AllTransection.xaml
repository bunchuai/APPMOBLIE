<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="APPMOBLIE.AllTransection"
             Title="Transection" 
             IconImageSource="iconstrans"
             xmlns:ic="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             xmlns:local="clr-namespace:APPMOBLIE">
    <AbsoluteLayout>
        <Image Source="background" Aspect="AspectFill"
                  AbsoluteLayout.LayoutBounds="0,0,1,0.15"
                   AbsoluteLayout.LayoutFlags="All" />
        <ic:CircleImage Source="kodelogo.jpg" 
                           Aspect="AspectFill"
                                HeightRequest="80" WidthRequest="80"
                           AbsoluteLayout.LayoutBounds="0.05,0.02"
                           AbsoluteLayout.LayoutFlags="PositionProportional"/>
        <Label x:Name="Username" HorizontalOptions="Center" 
                      FontFamily="{StaticResource Kanit}"
                       FontSize="16"
                        AbsoluteLayout.LayoutBounds="0.6,0.05"
                   AbsoluteLayout.LayoutFlags="PositionProportional"
                   FontAttributes="Bold"
                   />

        <ScrollView
                 AbsoluteLayout.LayoutBounds="0,1,1,0.85"
                   AbsoluteLayout.LayoutFlags="PositionProportional,SizeProportional">
            <StackLayout Padding="13" Spacing="13" BackgroundColor="#eeeeee">
                <Label Text="DateTime Today"  Style="{StaticResource Normalkanit}"/>
                <StackLayout Orientation="Vertical"
                             VerticalOptions="StartAndExpand"
                             BackgroundColor="#eeeeee">
                    <Button Text="stock in" BackgroundColor="White" BorderWidth="6" BorderColor="#c6e2ff" 
                                    CornerRadius="15"
                                    Clicked="Button_Clicked"
                            TextColor="#4ca3dd"
                            Style="{StaticResource Normalkanit}"
                            HeightRequest="80"
                              FontSize="25"
                            ></Button>
                    <ListView x:Name="Dashboardin" HasUnevenRows="True" IsVisible="{Binding Enabledashboard}" BackgroundColor="White" HeightRequest="450">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell IsEnabled="False">
                                    <StackLayout Orientation="Vertical"  Padding="20">
                                        <Label Text="{Binding reference, StringFormat='Ref.No : {0}'}" Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="StartAndExpand"/>
                                        <StackLayout Orientation="Horizontal">
                                            <Label Text="{Binding TransactionDate, StringFormat='Date : {0}'}" 
                                                   Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="StartAndExpand"/>

                                            <StackLayout Orientation="Horizontal">
                                                <Label Text="{Binding TransactionType}" Style="{x:StaticResource Normalkanit}"
                                                       x:Name="TypeTrans"
                                                  HorizontalOptions="EndAndExpand" />
                                                <Label Text="{Binding Quantity}"  Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="End"/>
                                                <Label Text="{Binding Unit}" Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="StartAndExpand"/>
                                            </StackLayout>
                                        </StackLayout>
                                        <Label Text="{Binding Location, StringFormat='Location on Store : {0} '}" Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="StartAndExpand"/>
                                        <Label Text="{Binding Username , StringFormat='Record By : {0}'}" Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="StartAndExpand"/>
                                    </StackLayout>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <StackLayout  VerticalOptions="CenterAndExpand">
                        <Button Text="stock out" BackgroundColor="White" BorderWidth="6" BorderColor="#c6e2ff" 
                                    CornerRadius="15"
                                    Clicked="Button_Clicked2"
                            TextColor="#4ca3dd"
                                FontSize="25"
                            Style="{StaticResource Normalkanit}"
                                HeightRequest="80"
                            ></Button>
                        <ListView x:Name="Dashboardout" HasUnevenRows="True" IsVisible="{Binding Enabledashboard}" BackgroundColor="White" HeightRequest="450">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <ViewCell IsEnabled="False" >
                                        <StackLayout Orientation="Vertical"  Padding="20">
                                            <Label Text="{Binding reference, StringFormat='Ref.No : {0}'}" Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="StartAndExpand"/>
                                            <StackLayout Orientation="Horizontal">
                                                <Label Text="{Binding TransactionDate, StringFormat='Date : {0}'}" 
                                                   Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="StartAndExpand"/>

                                                <StackLayout Orientation="Horizontal">
                                                    <Label Text="{Binding TransactionType}" Style="{x:StaticResource Normalkanit}"
                                                       x:Name="TypeTrans"
                                                  HorizontalOptions="EndAndExpand" />
                                                    <Label Text="{Binding Quantity}"  Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="End"/>
                                                    <Label Text="{Binding Unit}" Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="StartAndExpand"/>
                                                </StackLayout>
                                            </StackLayout>
                                            <Label Text="{Binding Location, StringFormat='Location on Store : {0} '}" Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="StartAndExpand"/>
                                            <Label Text="{Binding Username , StringFormat='Record By : {0}'}" Style="{x:StaticResource Normalkanit}"
                                               HorizontalOptions="StartAndExpand"/>
                                        </StackLayout>
                                    </ViewCell>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackLayout>
                </StackLayout>
               
            </StackLayout>
            
        </ScrollView>
    </AbsoluteLayout>
</ContentPage>